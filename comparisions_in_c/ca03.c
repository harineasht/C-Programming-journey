// Checking foor equality
#include <stdio.h>
int main()
{
    int num;
    scanf("%d", &num);
    if (num < 50)
    {
        printf("Success");
    }
    else
    {
        printf("Failure");
    }

    return 0;
}